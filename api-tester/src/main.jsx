import React,{useRef,useState} from "react";import{createRoot}from"react-dom/client";import"./style.css";const API_BASE=(import.meta.env.VITE_API_URL||"https://convertflow-backend.onrender.com/api").replace(/\/$/,"");const formats=["jpg","png","webp","gif","pdf","mp3","wav","mp4","webm"];
function App(){const fileRef=useRef(null);const[apiKey,setApiKey]=useState("");const[format,setFormat]=useState("jpg");const[dragging,setDragging]=useState(false);const[file,setFile]=useState(null);const[status,setStatus]=useState(null);const[testing,setTesting]=useState(false);const[metrics,setMetrics]=useState(null);function chooseFile(next){const selected=next?.[0];if(!selected)return;if(selected.size>100*1024*1024)return setStatus({type:"error",text:"That file is larger than the 100 MB API limit."});setFile(selected);setStatus(null);setMetrics(null)}function clearSession(){setApiKey("");setFile(null);setMetrics(null);setStatus({type:"info",text:"Test session cleared. The API key is no longer in memory."});if(fileRef.current)fileRef.current.value=""}async function testApi(){if(!apiKey.trim())return setStatus({type:"error",text:"Enter your ConvertFlow API key first."});if(!file)return setStatus({type:"error",text:"Choose a file to test the API."});setTesting(true);setStatus({type:"working",text:"Uploading and converting..."});const started=performance.now();try{const form=new FormData();form.append("file",file);form.append("outputFormat",format);const response=await fetch(`${API_BASE}/conversion/convert`,{method:"POST",headers:{"X-ConvertFlow-API-Key":apiKey.trim()},body:form});const elapsed=Math.round(performance.now()-started);setMetrics({status:response.status,time:elapsed,type:response.headers.get("content-type")||"unknown",size:file.size});if(!response.ok){let message=`Request failed with HTTP ${response.status}.`;try{const data=await response.json();if(data?.message)message=data.message}catch{const text=await response.text().catch(()=>"");if(text)message=text.slice(0,300)}throw new Error(message)}const blob=await response.blob();const url=URL.createObjectURL(blob),link=document.createElement("a");link.href=url;link.download=`convertflow-api-test.${format}`;document.body.appendChild(link);link.click();link.remove();setTimeout(()=>URL.revokeObjectURL(url),1000);setStatus({type:"success",text:"API test passed. The converted file was downloaded."})}catch(error){setStatus({type:"error",text:error.message||"The API request failed."})}finally{setTesting(false)}}return <div className="tester-shell"><header className="tester-header"><div className="brand-mark">CF</div><div><strong>ConvertFlow</strong><span>API Test Console</span></div><a href="https://convertflow-seven-delta.vercel.app/" target="_blank" rel="noreferrer">Main website ↗</a></header><main className="console"><section className="intro"><div className="eyebrow">DEVELOPER CONSOLE</div><h1>Test your API key.</h1><p>Run a real conversion request against the live ConvertFlow API without installing anything.</p></section><section className="panel"><div className="panel-title"><span>01</span><div><h2>API credentials</h2><p>Your key exists only in this page session.</p></div></div><label className="field-label" htmlFor="api-key">ConvertFlow API key</label><div className="key-row"><input id="api-key" className="key-input" type="password" value={apiKey} onChange={e=>setApiKey(e.target.value)} placeholder="cf_live_..." autoComplete="off" spellCheck="false"/><button className="secondary-button" onClick={clearSession}>Clear</button></div><div className="privacy-note"><span>●</span> Nothing is saved to localStorage, cookies, or the server. Refreshing clears the key.</div></section><section className="panel"><div className="panel-title"><span>02</span><div><h2>Conversion test</h2><p>Upload a file and test the same endpoint used by API customers.</p></div></div><div className={`dropzone ${dragging?"dragging":""} ${file?"has-file":""}`} onDragOver={e=>{e.preventDefault();setDragging(true)}} onDragLeave={()=>setDragging(false)} onDrop={e=>{e.preventDefault();setDragging(false);chooseFile(e.dataTransfer.files)}} onClick={()=>fileRef.current?.click()}><input ref={fileRef} type="file" hidden onChange={e=>chooseFile(e.target.files)}/><div className="upload-icon">↑</div>{file?<><strong>{file.name}</strong><span>{(file.size/1024/1024).toFixed(2)} MB · Click to replace</span></>:<><strong>Drop a file here</strong><span>or click to browse · maximum 100 MB</span></>}</div><div className="controls"><div className="format-control"><label className="field-label" htmlFor="format">Output format</label><select id="format" value={format} onChange={e=>setFormat(e.target.value)}>{formats.map(item=><option key={item} value={item}>{item.toUpperCase()}</option>)}</select></div><button className="run-button" disabled={testing} onClick={testApi}>{testing?"Testing API...":"Run API test →"}</button></div>{metrics&&<div className="metrics"><div><b>{metrics.status}</b><span>HTTP status</span></div><div><b>{metrics.time} ms</b><span>Response time</span></div><div><b>{(metrics.size/1024/1024).toFixed(2)} MB</b><span>Upload size</span></div><div><b>{metrics.type.split(";")[0]}</b><span>Response type</span></div></div>}{status&&<div className={`status ${status.type}`} role="status">{status.type==="success"?"✓":status.type==="error"?"!":"…"}<span>{status.text}</span></div>}</section><section className="how-it-works"><div><span>1</span><p>Enter your API key.</p></div><div><span>2</span><p>Upload a file and select the output format.</p></div><div><span>3</span><p>Inspect response metrics and download the result.</p></div></section></main><footer>ConvertFlow API Test Console · No API keys are persisted.</footer></div>}createRoot(document.getElementById("root")).render(<App/>);
