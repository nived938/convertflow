import { useEffect,useState } from "react";
import { Wrench,DownloadCloud,CheckCircle2,EyeOff } from "lucide-react";
import { useNavigate } from "react-router-dom";
const API=(import.meta.env.VITE_API_URL||"https://convertflow-backend.onrender.com/api").replace(/\/$/,"");
export default function AdminControlEnhancements(){const nav=useNavigate();const[token]=useState(()=>sessionStorage.getItem("convertflow_admin_token")||"");const[enabled,setEnabled]=useState(true);const[busy,setBusy]=useState(false);useEffect(()=>{if(!token)return;fetch(`${API}/admin/download-section/status`,{headers:{Authorization:`Bearer ${token}`}}).then(r=>r.json()).then(d=>setEnabled(d.enabled!==false)).catch(()=>{})},[token]);const toggle=async e=>{const next=e.target.checked;setBusy(true);try{const r=await fetch(`${API}/admin/download-section`,{method:"PATCH",headers:{"Content-Type":"application/json",Authorization:`Bearer ${token}`},body:JSON.stringify({enabled:next})});const d=await r.json();if(!r.ok||d.success===false)throw Error(d.message||"Could not update setting");setEnabled(d.enabled!==false)}catch(err){setEnabled(!next);window.alert(err.message)}finally{setBusy(false)}};if(!token)return null;return <section className="cf-admin-enhancements"><div className="cf-admin-enhancements-copy"><span className="section-label">PUBLIC WEBSITE CONTROL</span><h2>Download ConvertFlow</h2><p>Control the download section shown on the homepage. Disabling this hides the entire section from visitors without affecting the <code>/download</code> page.</p><div className={`cf-admin-status ${enabled?"is-on":"is-off"}`}>{enabled?<CheckCircle2 size={16}/>:<EyeOff size={16}/>} {enabled?"Visible on homepage":"Hidden from homepage"}</div></div><div className="cf-admin-enhancements-actions"><label className="cf-admin-switch"><input type="checkbox" checked={enabled} disabled={busy} onChange={toggle}/><span className="cf-admin-switch-track"><i/></span><span>{enabled?"Enabled":"Disabled"}</span></label><button className="cf-admin-tool-button" onClick={()=>nav("/admin/control/tools")}><Wrench size={17}/> Tool Utility</button><button className="cf-admin-download-link" onClick={()=>nav("/download")}><DownloadCloud size={17}/> Preview download page</button></div></section>}
