import {useState} from "react";
import Header from "../components/Header";
import Footer from "../components/Footer";
const API=(import.meta.env.VITE_API_URL||"https://convertflow-backend.onrender.com/api").replace(/\/$/,"");
export default function Contact(){const[rating,setRating]=useState(5),[message,setMessage]=useState(""),[sent,setSent]=useState(false),[busy,setBusy]=useState(false),[error,setError]=useState("");async function submit(e){e.preventDefault();setBusy(true);setError("");try{const r=await fetch(`${API}/feedback`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({rating,message,page:location.pathname})});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.message||"Could not send feedback.");setSent(true);setMessage("")}catch(e){setError(e.message)}finally{setBusy(false)}}return <div className="app"><Header/><main className="content-page"><span className="section-label">CONTACT</span><h1>Contact ConvertFlow</h1><p>Found a conversion problem, missing format, or website issue?</p><div className="info-card"><h2>Share your experience</h2>{sent?<><p>Thanks. Your feedback was sent successfully.</p><button className="auth-submit" style={{width:"auto"}} onClick={()=>setSent(false)}>Send another</button></>:<form onSubmit={submit}><p style={{marginBottom:10}}>How was your experience?</p><div className="feedback-rating">{[1,2,3,4,5].map(n=><button type="button" key={n} aria-label={`${n} stars`} className={n<=rating?"selected":""} onClick={()=>setRating(n)}>★</button>)}</div><textarea value={message} onChange={e=>setMessage(e.target.value)} required maxLength={2000} placeholder="Tell us what went well or what we should improve..." style={{display:"block",width:"100%",minHeight:130,boxSizing:"border-box",marginTop:16,padding:14,borderRadius:12,border:"1px solid #273244",background:"rgba(255,255,255,.03)",color:"inherit",resize:"vertical"}}/><button className="auth-submit" style={{width:"auto",marginTop:16}} disabled={busy}>{busy?"Sending...":"Send feedback"}</button>{error&&<p style={{color:"#fca5a5"}}>{error}</p>}</form>}</div><div className="info-card"><h2>Email</h2><p>Email the ConvertFlow team at <a href="mailto:nivedsreejaharidasan@gmail.com">nivedsreejaharidasan@gmail.com</a>.</p><p>When reporting a problem, include the input format, requested output format, and error message if one is shown. Please do not send private or sensitive files.</p></div></main><Footer/></div>}
