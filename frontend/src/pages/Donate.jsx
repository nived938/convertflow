import { useEffect,useState } from "react";
import Header from "../components/Header";
import Footer from "../components/Footer";
const API=(import.meta.env.VITE_API_URL||"https://convertflow-backend.onrender.com/api").replace(/\/$/,"");
const BMC_URL="https://www.buymeacoffee.com/powerplayexe";
export default function Donate(){const[name,setName]=useState("");const[started,setStarted]=useState(false);const[supporters,setSupporters]=useState([]);const[error,setError]=useState("");useEffect(()=>{fetch(`${API}/supporters`).then(r=>r.ok?r.json():null).then(d=>setSupporters(d?.supporters||[])).catch(()=>{});const widget=document.createElement("script");widget.dataset.name="BMC-Widget";widget.dataset.cfasync="false";widget.src="https://cdnjs.buymeacoffee.com/1.0.0/widget.prod.min.js";widget.dataset.id="powerplayexe";widget.dataset.description="Support ConvertFlow on Buy Me a Coffee";widget.dataset.message="Thanks for supporting ConvertFlow!";widget.dataset.color="#FFDD00";widget.dataset.position="Right";widget.dataset.x_margin="18";widget.dataset.y_margin="18";widget.async=true;document.body.appendChild(widget);return()=>{widget.remove();document.getElementById("bmc-wbtn")?.remove()}},[]);async function donate(){const clean=name.trim();if(!clean)return;setError("");try{const r=await fetch(`${API}/donations`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name:clean})});const d=await r.json();if(!r.ok||d.success===false)throw new Error(d.message||"Could not save your name.");localStorage.setItem("convertflow_donor_name",clean);setStarted(true);window.open(BMC_URL,"_blank","noopener,noreferrer")}catch(e){setError(e.message)}}return <div className="app"><Header/><main className="donate-page"><section className="donate-hero"><div className="donate-copy"><span className="section-label">KEEP CONVERTFLOW FREE</span><h1>Support the tools you use.</h1><p>ConvertFlow is free to use. Your support helps cover infrastructure and add more conversion formats.</p><div className="donate-benefits"><span>✓ Free conversions stay free</span><span>✓ More formats and improvements</span><span>✓ No subscription required</span></div><label htmlFor="donor-name">Your name</label><input id="donor-name" value={name} onChange={e=>setName(e.target.value)} placeholder="Enter your name" maxLength={80}/><button className="auth-submit" type="button" onClick={donate} disabled={!name.trim()}>☕ Continue to Buy Me a Coffee</button>{started&&<p className="donate-note">Thanks, {name.trim()}! Your name was sent to the ConvertFlow admin.</p>}{error&&<p className="donate-note" style={{color:"#f87171"}}>{error}</p>}<p className="donate-note">Your name is sent to ConvertFlow only so the admin can review it. Buy Me a Coffee handles the payment.</p></div><aside className="donate-qr-card"><span className="section-label">OUR SUPPORTERS</span>{supporters.length?<div style={{display:"grid",gap:8,marginTop:16}}>{supporters.map(s=><div key={s.id} style={{padding:"10px 12px",borderRadius:12,background:"rgba(255,255,255,.04)",border:"1px solid rgba(148,163,184,.12)",fontWeight:700}}>{s.name}</div>)}</div>:<p>No supporters added yet. Be the first to support ConvertFlow.</p>}<div style={{marginTop:22,paddingTop:18,borderTop:"1px solid rgba(148,163,184,.12)"}}><h2>Thank you ❤️</h2><p>Every supporter helps keep ConvertFlow online and improving.</p></div></aside></section></main><Footer/></div>}
